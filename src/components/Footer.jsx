export default function Footer() {
  return (
    <footer className="site-footer">
      <div className="site-footer-inner">
        <span className="text-mono text-tertiary">HAFIZZAN KADIR — © {new Date().getFullYear()}</span>
        <span className="text-mono text-tertiary">TRADER / ANALYST / BUILDER</span>
      </div>
    </footer>
  );
}
